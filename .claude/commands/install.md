Follow the "First-Time Setup" procedure in the project's CLAUDE.md file.
Work through each step in order. For each step, briefly explain what
you're about to do before doing it. Pause and ask for confirmation
before any step that modifies system settings (Homebrew install,
defaults write commands, symlink creation). If the user is running
with --dangerously-skip-permissions, proceed automatically but still
explain each step as you go.
