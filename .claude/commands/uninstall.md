Follow the "Uninstall" procedure in the project's CLAUDE.md file.
Before removing anything, check the current system state: verify the
symlink exists and points to this repo, check if the boom alias is
in ~/.zshrc, check current defaults values. Only remove what was
actually set up. Explain each change before making it.
